
    /** @type {import('next').NextConfig} */
    const nextConfig = {
      reactStrictMode: true,
      images: {
        domains: ["www.recruit.so", "www.reframe.so", "reframe.so"],
      },
    };
  
    module.exports = nextConfig;
  
    